---
name: init-project
description: >-
  Initialize comprehensive project documentation through iterative Q&A.
  Generates PRD with functional/non-functional requirements, architecture,
  and development phases. Creates project-specific coding rules.
  Use when starting a new project or regenerating project documentation.
disable-model-invocation: true
---

# Phase: Init Project

Read and follow the instructions in `.agents/phases/init-project.md`.

Use `.agents/templates/prd.md` as the document structure template for generating `docs/prd.md`.

Generate domain-specific rules in `.agents/rules/` and update `AGENTS.md` with references to them.
